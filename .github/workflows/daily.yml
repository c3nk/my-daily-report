name: Daily Report

on:
  workflow_dispatch:

jobs:
  build-run-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Debug REPO
        env:
          REPO: ${{ github.repository }}
        run: |
          echo "=== REPO ==="
          echo "$REPO"

      - name: Build Docker image
        run: docker build -t daily-report .

      - name: Run container
        run: docker run -v ${{ github.workspace }}:/app daily-report

      - name: Hardcoded token push test
        env:
          REPO: ${{ github.repository }}
        run: |
          # ✅ BURAYA kendi token'ını birebir YAPIŞTIR
          HARDCODE_PAT="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"

          echo "=== HARD TEST ==="
          echo "https://x-access-token:${HARDCODE_PAT}@github.com/${REPO}.git"

          git config --global user.name "actions-bot"
          git config --global user.email "actions-bot@example.com"

          git checkout --orphan gh-pages
          git rm -rf . || true

          echo "hello, this is test file" > index.csv
          git add index.csv
          git commit -m "Hardcoded push test"

          git remote remove origin || true

          # 🔑 Çift tırnak, prefix sabit
          git remote add secure "https://x-access-token:${HARDCODE_PAT}@github.com/${REPO}.git"

          git remote -v

          git push --force secure gh-pages
